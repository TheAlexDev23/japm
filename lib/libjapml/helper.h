#ifndef _HELPER_H_INCLUDED
#define _HELPER_H_INCLUDED

/*
* A collection of helper functions that don't belong in a separate file
*/

// You must free the result if result is non-NULL.
char* japml_str_replace(char *orig, char *rep, char *with);

#endif